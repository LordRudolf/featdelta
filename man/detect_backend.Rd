% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{detect_backend}
\alias{detect_backend}
\title{Detect database dialect from a DBI connection}
\usage{
detect_backend(con)
}
\arguments{
\item{con}{A \code{DBIConnection} object.}
}
\value{
Character scalar identifying the dialect:
one of \code{"postgres"}, \code{"sqlite"}, \code{"mysql"}, or \code{"unknown"}.
}
\description{
Attempts to infer the database backend (dialect) from DBI metadata and/or
the class of a DBI connection. Used to select dialect-specific SQL.
}
\details{
Detection uses (1) \code{DBI::dbGetInfo(con)$dbms.name} when available, and
falls back to connection class names (e.g. \code{PqConnection}, \code{SQLiteConnection},
\code{MariaDBConnection}) if metadata is unavailable or inconclusive.

If detection fails, returns \code{"unknown"}; callers can decide whether to error.
}
\keyword{internal}
