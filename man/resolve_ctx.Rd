% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_ctx.R
\name{resolve_ctx}
\alias{resolve_ctx}
\title{Resolve a featdelta execution context}
\usage{
resolve_ctx(
  con_or_fd,
  ...,
  raw_table = NULL,
  feat_table_name = NULL,
  key = NULL,
  dialect = NULL,
  meta_enabled = NULL,
  meta_schema = NULL
)
}
\arguments{
\item{con_or_fd}{Either a live \code{DBIConnection} or a featdelta connection
object such as one produced by \code{fd_connect()}.}

\item{...}{Reserved for future internal extensions passed through to
\code{make_ctx_from_args()}.}

\item{raw_table}{Optional character scalar overriding the raw/source table
name stored in the context.}

\item{feat_table_name}{Optional character scalar overriding the feature
table name stored in the context.}

\item{key}{Optional character scalar overriding the primary key stored in
the context.}

\item{dialect}{Optional character scalar overriding the detected dialect.
If \code{NULL}, the dialect is inferred from \code{con}.}

\item{meta_enabled}{Optional logical overriding metadata enablement.}

\item{meta_schema}{Optional list overriding metadata schema settings.}
}
\value{
A validated context object of class \code{"featdelta_ctx"}. Depending on
the supplied fields, the object may contain:
\itemize{
\item \code{con}
\item \code{dialect}
\item \code{raw_table}, \code{raw_table_id}, \code{raw_table_q}
\item \code{feat_table_name}, \code{feat_table_id}, \code{feat_table_q}
\item \code{key}, \code{key_q}
\item \code{meta_enabled}, \code{meta_schema}
}
}
\description{
Central internal entry point for DB-facing featdelta functions.
\code{resolve_ctx()} normalizes either a plain \code{DBIConnection} or a previously
constructed featdelta connection/context object into a validated
\code{featdelta_ctx}.
}
\details{
This function is intended to be called at the beginning of exported
DB-facing functions such as \code{fd_fetch()}, \code{fd_upsert()}, and \code{fd_run()}.
The returned context should then be used as the single source of truth for
database identity fields instead of reparsing table names or requoting
identifiers in downstream code.

Context resolution has two stages:
\enumerate{
\item normalize raw inputs and explicit overrides;
\item compute stable derived fields (for example parsed table identifiers and
quoted names) and validate the final object.
}

Pure in-memory functions such as \code{fd_define()} and \code{fd_compute()} do not
use this function.

Explicit arguments take precedence over stored values in a
\code{"featdelta_con"} object. When this happens, the context is rebuilt so that
all derived fields remain consistent with the overridden raw fields.

The context stores only stable derived values. Operation-specific or
mutable state such as whether a table currently exists, chunk sizes, or
cleaned SQL strings should not be stored in the context.
}
\seealso{
Other featdelta context helpers: 
\code{\link{ctx_apply_overrides}()},
\code{\link{ctx_build_derived}()},
\code{\link{ctx_validate}()},
\code{\link{fd_connect}()},
\code{\link{make_ctx_from_args}()}
}
\concept{featdelta context helpers}
\keyword{internal}
